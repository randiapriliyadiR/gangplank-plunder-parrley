# Gangplank Plunder Parrrley — MT5 presets (`.set`)

Load in MT5: attach EA → **Inputs** → **Load** → pick a file from this folder (or copy into `MQL5/Presets/`).

| File | Use case | Notes |
|------|----------|-------|
| `GPP_XAU_H4_PropPass.set` | Prop firm challenge | H4 · risk **1%** · daily **3%** / max **10%** / target **+10%** — verified **PASS** 2023.03.13 (~$110k) |

Chart: any `XAUUSD*` symbol (e.g. `XAUUSD_ExnessPro`). Template TF is **H4** inside the preset.
